name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libsdl2-dev xvfb

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install sdl2

      - name: Build
        run: make

      - name: Test (Linux)
        if: runner.os == 'Linux'
        run: |
          xvfb-run ./build/test_textbounds
          ./build/test_layout
          ./build/test_xml_parser
          ./build/test_image
          ./build/test_highlight

      - name: Test (macOS)
        if: runner.os == 'macOS'
        run: |
          ./build/test_textbounds
          ./build/test_layout
          ./build/test_xml_parser
          ./build/test_image
          ./build/test_highlight
